% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{gaussian_mean_posterior}
\alias{gaussian_mean_posterior}
\title{Closed-form posterior of the Gaussian mean}
\usage{
gaussian_mean_posterior(data, sigma)
}
\arguments{
\item{data}{Numeric vector of observations.}

\item{sigma}{Positive finite scalar, the known standard deviation.}
}
\value{
A list with the posterior \code{mean} and standard deviation \code{sd}.
}
\description{
Returns the analytic posterior of the mean \code{mu} for observations following
\code{Normal(mu, sigma^2)} with known \code{sigma} and a flat prior on \code{mu}. The
posterior is \code{Normal(mean(data), sigma^2 / length(data))}. It provides the
ground truth used to check that \code{\link[=metropolis_gaussian_mean]{metropolis_gaussian_mean()}} recovers known
parameters.
}
\examples{
set.seed(1)
gaussian_mean_posterior(rnorm(500, mean = 3, sd = 2), sigma = 2)

}
