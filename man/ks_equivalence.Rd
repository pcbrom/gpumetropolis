% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ks_equivalence}
\alias{ks_equivalence}
\title{Distributional equivalence by the two-sample Kolmogorov-Smirnov test}
\usage{
ks_equivalence(x, y, warmup = NULL, alpha = 0.05, thin = TRUE)
}
\arguments{
\item{x, y}{Each a \code{gpumetropolis_fit} object, an \code{n_iter} by \code{n_chains}
numeric matrix, or a numeric vector of draws.}

\item{warmup}{Number of initial iterations discarded from \code{x} and \code{y} before
pooling. When \code{NULL}, half of the iterations are discarded for matrix and
fit inputs, and none for vector inputs.}

\item{alpha}{Significance level of the test. Default 0.05.}

\item{thin}{Controls thinning before the test. \code{TRUE} (default) thins each
pooled sample to its effective sample size. A positive integer keeps every
\code{thin}-th draw. \code{FALSE} applies no thinning.}
}
\value{
A list with the test \code{statistic}, the \code{p_value}, the chosen \code{alpha},
a logical \code{equivalent} (\code{TRUE} when \code{p_value > alpha}) and the post-thinning
pooled sample sizes \code{n_x} and \code{n_y}.
}
\description{
Pools the post-warmup draws of two samplers and applies the two-sample
Kolmogorov-Smirnov test. It is the equivalence gate for the package: a CPU
and a GPU sampler are treated as equivalent when the test does not reject
the hypothesis that their draws come from the same distribution.
}
\details{
The Kolmogorov-Smirnov test assumes independent draws, while Markov chain
Monte Carlo output is autocorrelated; feeding it raw draws makes the test
reject far too often. By default the pooled draws are therefore thinned down
to the effective sample size returned by \code{\link[=ess]{ess()}} before the test is applied.

Not rejecting is evidence consistent with equivalence, not a proof that the
two distributions are identical. Report the statistic and the p-value, and
read \code{equivalent} as "no detected difference at level \code{alpha}".
}
\examples{
set.seed(1)
x <- rnorm(800, mean = 1, sd = 1)
a <- metropolis_gaussian_mean(x, sigma = 1, n_iter = 2000, seed = 1)
b <- metropolis_gaussian_mean(x, sigma = 1, n_iter = 2000, seed = 2)
ks_equivalence(a, b)

}
