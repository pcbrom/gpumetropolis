% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{gpum_fit_catalog}
\alias{gpum_fit_catalog}
\title{Automatic marginal distribution selection for one column}
\usage{
gpum_fit_catalog(
  data,
  catalog = "auto",
  ranking = c("waic", "loo"),
  n_iter = 8000L,
  n_chains = 4L,
  seed = 1L,
  backend = "cpu",
  ...
)
}
\arguments{
\item{data}{A numeric vector, or a one-column data frame or matrix.}

\item{catalog}{Either \code{"auto"} (default, eligible families chosen from the
detected support and modality) or a character vector of family names to
fit. Available families: \code{normal}, \code{student_t}, \code{logistic}, \code{laplace},
\code{gamma}, \code{lognormal}, \code{weibull}, \code{exponential}, \code{beta}, \code{gmm2}.}

\item{ranking}{Selection criterion, \code{"waic"} (default) or \code{"loo"}.}

\item{n_iter, n_chains, seed, backend, ...}{Passed to \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}
}
\value{
An object of class \code{gpum_catalog}: a list with the \code{best} family
name and its \code{fit}, the \code{comparison} ranking table, the support and
modality verdict, and the fitted CDF of the winner.
}
\description{
Detects the support and the modality of a numeric column, fits every
eligible parametric family with the sampler, and ranks them by predictive
comparison. The winner's fitted cumulative distribution is the
probability-integral transform used to feed \code{\link[=gpum_copula]{gpum_copula()}}, so the
explicit marginals and the copula together model the full joint
distribution (Sklar's theorem).
}
\details{
Every family is fit in an unconstrained parametrisation, so the sampler
never proposes an invalid parameter; the natural parameters are recovered
for reporting. A column detected multimodal brings the two-component
Gaussian mixture into the eligible set, which is fit with parallel
tempering (\code{method = "pt"}) and relabeled so the lower-location component
is first, since a random walk cannot cross the modes.
}
\examples{
set.seed(1)
y <- rgamma(400, shape = 3, rate = 2)
result <- gpum_fit_catalog(y, n_iter = 4000, n_chains = 4, backend = "cpu")
result

}
\seealso{
\code{\link[=gpu_metropolis]{gpu_metropolis()}}, \code{\link[=gpum_copula]{gpum_copula()}}, \code{\link[=marginal_cdf]{marginal_cdf()}}
}
