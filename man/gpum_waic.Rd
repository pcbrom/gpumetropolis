% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{gpum_waic}
\alias{gpum_waic}
\title{Widely applicable information criterion (WAIC)}
\usage{
gpum_waic(fit, data, max_draws = 4000L)
}
\arguments{
\item{fit}{A \code{gpum_fit} from \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}

\item{data}{The same data passed to \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}

\item{max_draws}{Cap on the number of posterior draws used, to bound the
pointwise matrix. Default 4000.}
}
\value{
An object of class \code{gpum_waic} with \code{waic}, \code{elpd_waic} (the
expected log pointwise predictive density), \code{p_waic} (the effective number
of parameters), the standard error \code{se} and the \code{pointwise} terms.
}
\description{
Computes WAIC from the pointwise log-likelihood of the fit (Watanabe 2010;
Vehtari, Gelman and Gabry 2017). WAIC estimates out-of-sample predictive
accuracy without a marginal likelihood, so it is robust where Bayes factors
are prior-sensitive; lower WAIC is preferred when comparing models on the
same data.
}
\details{
The comparison is valid only when the log-likelihood is normalised. The
model DSL evaluates the log-density up to an additive constant, which is
enough for sampling a fixed model but not for comparing models with
different likelihoods, since the dropped constants differ. To compare
models with WAIC or LOO, write the full normalised per-observation
log-likelihood in the formula, including the normalising constant such as
\code{-0.5 * log(2 * pi * sigma^2)} for a Gaussian.
}
\seealso{
\code{\link[=gpum_loo]{gpum_loo()}}, \code{\link[=gpum_bayes_factor]{gpum_bayes_factor()}}
}
