% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{gpum_bayes_factor}
\alias{gpum_bayes_factor}
\title{Bayes factor between two models by thermodynamic integration}
\usage{
gpum_bayes_factor(model1, model0, data, ...)
}
\arguments{
\item{model1, model0}{Two \code{gpum_model} objects with proper priors, the
alternative and the null.}

\item{data}{The data, shared by both models.}

\item{...}{Passed to \code{\link[=gpum_evidence]{gpum_evidence()}} for both models.}
}
\value{
An object of class \code{gpum_bayes_factor} with \code{bf10}, \code{log_bf10}, the
two log evidences and a verbal \code{interpretation} on the Jeffreys scale.
}
\description{
Computes the Bayes factor \code{B_10 = p(y | model1) / p(y | model0)} from the
log marginal likelihoods of the two models, each by \code{\link[=gpum_evidence]{gpum_evidence()}}. The
Bayes factor is the formal Bayesian weight of evidence for one model over
another; both models must carry a proper prior.
}
\details{
The Bayes factor is sensitive to the priors, sharply so for a point null
under a diffuse prior, the Jeffreys-Lindley effect: a more diffuse prior on
the larger model penalises it regardless of the data. Report the priors with
the factor, and prefer \code{\link[=gpum_waic]{gpum_waic()}} or \code{\link[=gpum_loo]{gpum_loo()}} when the goal is
predictive comparison rather than a weight of evidence.
}
\seealso{
\code{\link[=gpum_evidence]{gpum_evidence()}}, \code{\link[=gpum_waic]{gpum_waic()}}, \code{\link[=gpum_loo]{gpum_loo()}}
}
