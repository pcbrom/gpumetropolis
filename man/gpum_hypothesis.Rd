% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{gpum_hypothesis}
\alias{gpum_hypothesis}
\title{Posterior probability of a hypothesis on one parameter}
\usage{
gpum_hypothesis(fit, parameter, lower = -Inf, upper = Inf)
}
\arguments{
\item{fit}{A \code{gpum_fit} from \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}

\item{parameter}{Name of the parameter.}

\item{lower, upper}{Bounds of the hypothesis interval. Defaults span the
real line, so \code{lower = 0} gives the one-sided \code{P(theta > 0 | y)} as
\code{prob}, the mass in the interval \verb{(0, Inf)}; \code{prob_below} is then the
complementary \code{P(theta <= 0 | y)}.}
}
\value{
An object of class \code{gpum_hypothesis} with \code{prob} (mass in the
interval), \code{prob_below}, \code{prob_above} and the inputs.
}
\description{
Reports the posterior probability that a parameter lies in an interval,
below it and above it, directly from the draws. This is an estimation
decision: it needs no marginal likelihood and inherits the robustness of
the posterior the sampler returned.
}
\seealso{
\code{\link[=gpum_rope]{gpum_rope()}}
}
