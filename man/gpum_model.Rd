% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{gpum_model}
\alias{gpum_model}
\title{Declare a model for the GPU-portable Metropolis sampler}
\usage{
gpum_model(loglik, params, data = character(0), prior = NULL)
}
\arguments{
\item{loglik}{One-sided formula, the per-observation log-likelihood, up to
an additive constant. It may reference the parameter names and the data
column names.}

\item{params}{Character vector of parameter names.}

\item{data}{Character vector of data column names. Empty for a model with
no data term.}

\item{prior}{One-sided formula, the joint log-prior over the parameters, up
to an additive constant. It may reference only the parameter names. \code{NULL}
is a flat prior.}
}
\value{
An object of class \code{gpum_model}.
}
\description{
Compiles a log-likelihood and an optional log-prior, declared as one-sided
formulas, into the bytecode the sampler runs. The log-likelihood is a
per-observation expression: the sampler sums it over the data. The formulas
may use \code{+}, \code{-}, \code{*}, \code{/}, \code{^}, unary \code{-}, and \code{exp}, \code{log}, \code{sqrt}; any
other symbol or function is rejected with a clear error.
}
\examples{
# Gaussian mean with known sd = 2 and a flat prior on mu.
m <- gpum_model(
  loglik = ~ -((y - mu)^2) / 8,
  params = "mu",
  data = "y"
)

}
\seealso{
\code{\link[=gpu_metropolis]{gpu_metropolis()}}
}
