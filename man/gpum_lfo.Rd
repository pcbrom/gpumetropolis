% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{gpum_lfo}
\alias{gpum_lfo}
\title{Exact leave-future-out cross-validation for dependent data}
\usage{
gpum_lfo(ts_model, L = NULL, n_iter = 4000L, n_chains = 4L, seed = 1L, ...)
}
\arguments{
\item{ts_model}{A \code{gpum_ts} from \code{\link[=gpum_ts_model]{gpum_ts_model()}}.}

\item{L}{Minimum training rows before the first evaluation: rows
\code{1:L} train the first refit and row \code{L + 1} is the first scored
point. Default keeps the last quarter of the rows for evaluation.}

\item{n_iter, n_chains, seed, ...}{Passed to \code{\link[=gpu_metropolis]{gpu_metropolis()}} for each
refit.}
}
\value{
A list of class \code{gpum_lfo} with \code{elpd_lfo} (the summed
expected log predictive density), \code{se} (its standard error over the
evaluated points), and \code{pointwise} (one row per evaluated time).
}
\description{
Estimates the expected log predictive density of one-step-ahead
forecasts by leave-future-out cross-validation with an expanding
window: for each held-out time \code{t} in the evaluation range, the model
is refit on rows \code{1:(t - 1)} and scored on the conditional log
predictive density of row \code{t}, integrated over the posterior by
averaging the pointwise likelihood across the draws. This is the
evaluation scheme that respects temporal ordering; \code{\link[=gpum_waic]{gpum_waic()}} and
\code{\link[=gpum_loo]{gpum_loo()}} assume exchangeable pointwise contributions and are not
valid for dependent rows (Burkner, Gabry and Vehtari 2020). The refits
are exact rather than importance-sampling approximations: the sampler
is fast enough that the approximation of Burkner, Gabry and Vehtari
(2020) buys nothing here.
}
\seealso{
\code{\link[=gpum_ts_model]{gpum_ts_model()}}, \code{\link[=gpum_waic]{gpum_waic()}}, \code{\link[=gpum_loo]{gpum_loo()}}
}
