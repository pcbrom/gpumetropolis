% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{gpu_metropolis}
\alias{gpu_metropolis}
\title{Run the GPU-portable batched Metropolis sampler}
\usage{
gpu_metropolis(
  model,
  data = NULL,
  init = NULL,
  proposal_sd = 0.1,
  n_iter = 2000L,
  n_chains = 4L,
  warmup = NULL,
  adapt = TRUE,
  method = c("rwm", "pt", "de"),
  temperatures = NULL,
  swap_every = NULL,
  gamma = NULL,
  de_noise = 0.001,
  de_every = NULL,
  seed = 1L,
  backend = c("auto", "cpu", "cuda", "vulkan")
)
}
\arguments{
\item{model}{A \code{gpum_model} object.}

\item{data}{A named list or data frame with one entry per data column named
in the model. Ignored for a model with no data term.}

\item{init}{Optional numeric matrix of starting values, \code{n_chains} rows by
one column per parameter. When supplied its row count sets the number of
chains. When \code{NULL}, chains start from independent standard normal draws.}

\item{proposal_sd}{Initial scale of the Gaussian random-walk proposal.
Accepts a scalar (recycled to all parameters and chains), a length-
\code{n_params} vector (broadcast across chains) or an \code{n_chains} by
\code{n_params} matrix (one row per chain). When \code{adapt = TRUE}, this is
only the warmup seed and adaptation refines it per chain.}

\item{n_iter}{Iterations the sampler runs per chain, counting warmup.
Default 2000.}

\item{n_chains}{Number of chains. Used only when \code{init} is \code{NULL}.
Default 4.}

\item{warmup}{Warmup iterations to discard before returning, following the
convention of Stan and nimble. Must lie in \verb{[0, n_iter)}. Default
\code{floor(n_iter / 2)}, so \code{fit$draws} is post-warmup and is suitable for
direct plotting. Set \code{warmup = 0} to keep every iteration, useful for
inspecting the burn-in trajectory in a trace plot.}

\item{adapt}{Whether to adapt the per-chain proposal scale during
warmup. When \code{TRUE} (default) and \code{warmup > 0}, the warmup runs in
batches; between batches, Welford updates the per-chain running
variance and Robbins-Monro updates the per-chain scalar toward the
asymptotic optimum acceptance (0.234 in \code{d >= 2}, 0.44 in \code{d = 1};
Roberts-Gelman-Gilks 1997, Roberts-Rosenthal 2009). Adaptation stops
cleanly at the end of warmup, so the sampling phase is stationary.
Set \code{FALSE} to keep the trim-only warmup of 0.1.x.}

\item{method}{Sampling method: \code{"rwm"} (default) is the random-walk
Metropolis with optional per-chain adaptation; \code{"pt"} is parallel
tempering with one chain per temperature on the same target and
adjacent-pair swap proposals between batches, where the cold chain
(\code{T = 1}) provides the posterior samples; \code{"de"} is Differential
Evolution MCMC, whose proposal for each chain is the scaled
difference of two other chains plus a small jitter, so the proposal
aligns with the correlation of the target through the ensemble
geometry without an explicit covariance (ter Braak 2006). The \code{"de"}
path needs at least 4 chains.}

\item{temperatures}{Numeric vector of length \code{n_chains}, the
temperature of each chain in \code{method = "pt"}. Ignored for the other
methods. When \code{NULL} (default), a geometric ladder from 1 to 10 is
used. The first entry must be 1 for the cold chain.}

\item{swap_every}{Integer, the iteration count between swap proposals
in \code{method = "pt"}. Ignored for the other methods. When \code{NULL}
(default), uses \code{max(n_chains, 10)}.}

\item{gamma}{Scale of the difference vector in \code{method = "de"}.
Ignored for the other methods. When \code{NULL} (default), uses
\code{2.38 / sqrt(2 * n_params)}, the value optimal for an approximately
Gaussian target (ter Braak 2006). With probability 0.1 each
iteration the scale collapses to 1 for an occasional mode-crossing
jump.}

\item{de_noise}{Non-negative factor for the per-dimension jitter added
to the \code{method = "de"} proposal, in units of the current per-chain
per-dimension scale. A small positive value keeps the chain
irreducible when the ensemble collapses onto a subspace. Default
\code{1e-3}. Ignored for the other methods.}

\item{de_every}{Integer, the iteration count between refreshes of the
frozen population snapshot used for the difference vectors in
\code{method = "de"}. Ignored for the other methods. When \code{NULL}
(default), uses \code{max(n_chains, 10)}.}

\item{seed}{Integer seed. Each chain advances its own counter-based stream
from a triple32 hash; the seed is itself hashed, so runs with consecutive
integer seeds get independent streams.}

\item{backend}{Compute backend: \code{"cpu"}, \code{"cuda"} (NVIDIA-native),
\code{"vulkan"} (vendor-agnostic, through wgpu), or \code{"auto"}. \code{"auto"}
selects \code{"cuda"} when its feature was compiled into the build, then
\code{"vulkan"}, and falls back to \code{"cpu"} with a one-shot per-session
message stating that no GPU backend is available. Default \code{"auto"}.}
}
\value{
An object of class \code{gpum_fit}: a list with \code{draws} (an
\code{n_iter - warmup} by \code{n_chains} by \code{n_params} array of post-warmup
samples), \code{accept_rate}, \code{n_iter} (kept count), \code{n_iter_total} (raw
count), \code{warmup} and the rest of the run metadata.
}
\description{
Advances many independent random-walk Metropolis chains over a model
declared with \code{\link[=gpum_model]{gpum_model()}}. The log-density kernel runs on the chosen
backend; the data is uploaded once and stays resident across the run.
}
\examples{
set.seed(1)
y <- rnorm(2000, mean = 3, sd = 2)
m <- gpum_model(~ -((y - mu)^2) / 8, params = "mu", data = "y")
fit <- gpu_metropolis(m, data = list(y = y), proposal_sd = 0.05,
                      n_iter = 1000, n_chains = 4)
rhat(fit$draws[, , 1])

}
\seealso{
\code{\link[=gpum_model]{gpum_model()}}, \code{\link[=rhat]{rhat()}}, \code{\link[=ess]{ess()}}
}
