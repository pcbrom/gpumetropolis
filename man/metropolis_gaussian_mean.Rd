% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampler.R
\name{metropolis_gaussian_mean}
\alias{metropolis_gaussian_mean}
\title{Batched random-walk Metropolis sampler for a Gaussian mean}
\usage{
metropolis_gaussian_mean(
  data,
  sigma,
  n_iter = 2000L,
  n_chains = 4L,
  init = NULL,
  proposal_sd = NULL,
  seed = 1L
)
}
\arguments{
\item{data}{Numeric vector of observations. Must be finite and non-empty.}

\item{sigma}{Positive finite scalar, the known standard deviation of the
observations.}

\item{n_iter}{Number of iterations recorded per chain. Default 2000.}

\item{n_chains}{Number of independent chains. Used only when \code{init} is
\code{NULL}. Default 4.}

\item{init}{Optional numeric vector of starting values, one per chain. When
supplied, its length sets the number of chains. When \code{NULL}, chains start
evenly spread over \verb{mean(data) +/- 2 * sigma}, which overdisperses them so
that \code{\link[=rhat]{rhat()}} is informative while staying bounded for many chains.}

\item{proposal_sd}{Positive finite scalar, the standard deviation of the
Gaussian random-walk proposal. When \code{NULL}, defaults to
\code{2.4 * sigma / sqrt(length(data))}, scaled to the width of the posterior.}

\item{seed}{Single integer seed. Each chain runs an independent PCG64 stream
derived from \verb{(seed, chain_index)}, so the result is reproducible and does
not depend on chain scheduling.}
}
\value{
An object of class \code{gpumetropolis_fit}: a list with \code{draws} (an
\code{n_iter} by \code{n_chains} numeric matrix), \code{accept_rate} (per-chain
acceptance rate) and the run metadata.
}
\description{
Samples the posterior of the mean \code{mu} of observations assumed to follow
\code{Normal(mu, sigma^2)} with known \code{sigma} and a flat prior on \code{mu}. Many
independent chains are advanced together: at every iteration the candidate
state of each chain is evaluated by a single batched call to the log-density
kernel.
}
\details{
This is the CPU reference sampler. Its log-density kernel runs on the CPU.
Later package versions dispatch the same kernel to a GPU and are validated
for distributional equivalence against this function with \code{\link[=ks_equivalence]{ks_equivalence()}}
and \code{\link[=rhat]{rhat()}}. The sequential dependence within a chain (\verb{x_\{t+1\}} depends on
\code{x_t}) is intrinsic to Markov chain Monte Carlo and is not parallelised; the
parallel axes are the chains and the sum over the data.
}
\examples{
set.seed(1)
x <- rnorm(500, mean = 3, sd = 2)
fit <- metropolis_gaussian_mean(x, sigma = 2, n_iter = 1000)
rhat(fit)

}
\seealso{
\code{\link[=rhat]{rhat()}}, \code{\link[=ess]{ess()}}, \code{\link[=ks_equivalence]{ks_equivalence()}}, \code{\link[=gaussian_mean_posterior]{gaussian_mean_posterior()}}
}
