% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{gpum_diagnose}
\alias{gpum_diagnose}
\title{One-call diagnostic: convergence stats, plots and verdict.}
\usage{
gpum_diagnose(fit, plot = TRUE, return_data = FALSE, crlb = NULL)
}
\arguments{
\item{fit}{A \code{gpum_fit} object from \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}

\item{plot}{Whether to open the diagnostic plot. Default \code{TRUE}.}

\item{return_data}{Whether to return the structured stats invisibly.
Default \code{FALSE}; the function then returns \code{NULL} invisibly.}

\item{crlb}{Optional \code{gpum_crlb} object from \code{\link[=gpum_crlb]{gpum_crlb()}}. When supplied
and applicable, the printout adds a posterior-sd against Cramer-Rao-bound
comparison and the density panels overlay the asymptotic-normal reference
from the inverse Fisher information. Default \code{NULL}.}
}
\value{
When \code{return_data = TRUE}, a list with \code{summary} (one row
per parameter), \code{verdict} (the convergence diagnosis and a
suggested next step), \code{adaptation} (passed through from
\code{fit$adaptation}) and \code{adaptation_hint} (a character string when
the last warmup batch closed below 80\% of the asymptotic target
acceptance, \code{NULL} otherwise). Otherwise \code{NULL} invisibly.
}
\description{
Produces a per-parameter table (mean, standard deviation, 2.5\%, 50\%
and 97.5\% quantiles, split R-hat, effective sample size and Monte
Carlo standard error) and a convergence verdict from the asymptotic
canonical thresholds (R-hat below 1.01 in every parameter and ESS at
or above 400). When \code{plot = TRUE}, opens a multi-panel plot per
parameter showing the trace, the pooled density, the running mean
per chain and the pooled autocorrelation; when the fit carries
adaptation book-keeping, an extra panel shows the per-chain
acceptance rate by warmup batch with the asymptotic optimum drawn as
a horizontal reference.
}
\seealso{
\code{\link[=gpu_metropolis]{gpu_metropolis()}}, \code{\link[=rhat]{rhat()}}, \code{\link[=ess]{ess()}}
}
