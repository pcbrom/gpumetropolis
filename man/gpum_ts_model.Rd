% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{gpum_ts_model}
\alias{gpum_ts_model}
\title{Declare a Markov (time-series) model by conditional factorisation}
\usage{
gpum_ts_model(
  loglik,
  params,
  series,
  order = 1L,
  covariates = NULL,
  prior = NULL
)
}
\arguments{
\item{loglik}{One-sided formula, the conditional log-density of the
current observation. Refer to the series by its name in \code{series} and
to lags as \verb{<name>_lag1}, ..., \verb{<name>_lagp}. Exogenous covariates
from \code{covariates} enter by their own names (aligned to the current
time index).}

\item{params}{Character vector of parameter names.}

\item{series}{Named list with one entry, the observed series
(\verb{list(y = <numeric>)}); the name is the symbol used in \code{loglik}.}

\item{order}{Markov order \code{p >= 1}, the number of lags the conditional
density uses.}

\item{covariates}{Optional named list of exogenous covariate vectors,
each the same length as the series; row \code{t} of the design carries the
covariate at time \code{t}.}

\item{prior}{Optional one-sided formula for the log-prior (plus, when
wanted, the stationary log-density of the initial state), evaluated
once per parameter vector.}
}
\value{
A list of class \code{gpum_ts} with the compiled \code{model} (a
\code{gpum_model}), the assembled \code{data} list ready for
\code{\link[=gpu_metropolis]{gpu_metropolis()}}, the \code{order}, and the effective sample rows
\code{n_rows = length(y) - order}.
}
\description{
Builds a \code{\link[=gpum_model]{gpum_model()}} for dependent data with a Markov dependence of
order \code{p}. The user writes the conditional log-density of one
observation given its \code{p} predecessors, referring to the current value
by the series name and to the lagged values as \verb{<name>_lag1} through
\verb{<name>_lagp}; \code{gpum_ts_model()} assembles the lagged design so the
engine's row sum computes the conditional log-likelihood
\verb{sum_\{t = p + 1\}^\{n\} log f(y_t | y_\{t-1\}, ..., y_\{t-p\})}.
}
\details{
The likelihood conditions on the first \code{p} observations, the standard
conditional-likelihood treatment (Billingsley 1961; Hamilton 1994,
section 5.2); for \code{n} moderately large the information in the first \code{p}
points is negligible. A stationary initial density, when available in
closed form, can be added through \code{prior}, which is evaluated once per
parameter vector.

Inference downstream is licensed by the dependent-data asymptotics: the
conditional score is a martingale difference sequence, so the observed
information of the conditional factorisation is the right curvature
(martingale CLT, Hall and Heyde 1980) and the posterior satisfies a
Bernstein-von Mises theorem for ergodic Markov processes (Borwanker,
Kallianpur and Prakasa Rao 1971). Model comparison by \code{\link[=gpum_waic]{gpum_waic()}} or
\code{\link[=gpum_loo]{gpum_loo()}} assumes exchangeable pointwise terms and does not transfer
to dependent rows; use \code{\link[=gpum_lfo]{gpum_lfo()}} instead.
}
\examples{
set.seed(1)
y <- as.numeric(arima.sim(list(ar = 0.6), n = 300))
ts_m <- gpum_ts_model(
  ~ -0.5 * ((y - mu - phi * (y_lag1 - mu)) / exp(ls))^2 - ls,
  params = c("mu", "phi", "ls"), series = list(y = y), order = 1,
  prior = ~ -0.5 * (mu / 10)^2 - 0.5 * (phi / 2)^2 - 0.5 * (ls / 2)^2
)
fit <- gpu_metropolis(ts_m$model, data = ts_m$data, n_iter = 2000,
                      n_chains = 4, seed = 1)

}
\seealso{
\code{\link[=gpum_model]{gpum_model()}}, \code{\link[=gpu_metropolis]{gpu_metropolis()}}, \code{\link[=gpum_lfo]{gpum_lfo()}}
}
