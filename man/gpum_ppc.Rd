% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{gpum_ppc}
\alias{gpum_ppc}
\title{Posterior predictive sample from a fit}
\usage{
gpum_ppc(fit, generate, n = 4000L)
}
\arguments{
\item{fit}{A \code{gpum_fit} from \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}

\item{generate}{A function of one named parameter vector (one posterior
draw) returning one or more simulated observations. It may close over the
data to simulate a covariate-dependent response.}

\item{n}{Number of posterior draws to simulate from. Default 4000.}
}
\value{
A numeric vector, the pooled posterior-predictive sample.
}
\description{
Draws \code{n} parameter vectors from the posterior (the cold chain under
parallel tempering) and, for each, calls \code{generate()} to simulate from the
model, returning the pooled posterior-predictive sample. The package does
not yet generate from an arbitrary likelihood on its own; the user supplies
the family's one-line simulator, which they already know from the model they
declared. An automatic generator from the model itself is the goal of the
synthesis release on the roadmap.
}
\seealso{
\code{\link[=gpum_density_compare]{gpum_density_compare()}}
}
