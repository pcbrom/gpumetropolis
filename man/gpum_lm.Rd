% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conjugate.R
\name{gpum_lm}
\alias{gpum_lm}
\title{Declare a conjugate Gaussian linear model}
\usage{
gpum_lm(formula, data, b0 = 0, B0 = 1e-06, c0 = 0.002, d0 = 0.002)
}
\arguments{
\item{formula}{A standard R model formula, e.g. \code{mpg ~ wt}. Interactions
and transformations follow the usual \code{model.matrix} rules.}

\item{data}{A data frame holding the variables of \code{formula}.}

\item{b0}{Prior mean of \code{beta}; scalar recycled or a vector of length
equal to the number of columns of the design matrix. Default 0.}

\item{B0}{Prior precision of \code{beta} (inverse covariance, scaled by
\code{sigma^2}); a scalar for \code{B0 * I} or a full matrix. Default \code{1e-6},
near-flat but proper.}

\item{c0}{Prior shape times two of \code{sigma^2}: \code{sigma^2 ~ InvGamma(c0 / 2, d0 / 2)}. Default 0.002.}

\item{d0}{Prior rate times two of \code{sigma^2}. Default 0.002.}
}
\value{
An object of class \code{gpum_conjugate} holding the design matrix,
the response and the prior, ready for \code{\link[=gpu_metropolis]{gpu_metropolis()}}.
}
\description{
Builds the conjugate specification of the Gaussian linear model
\verb{y = X beta + eps}, \verb{eps ~ N(0, sigma^2 I)}, under the
Normal-inverse-Gamma prior
\verb{beta | sigma^2 ~ N(b0, sigma^2 B0^\{-1\})},
\code{sigma^2 ~ InvGamma(c0 / 2, d0 / 2)},
with \code{B0} a prior precision matrix. Under this prior the joint posterior
of \verb{(beta, sigma^2)} is again Normal-inverse-Gamma in closed form, so
\code{\link[=gpu_metropolis]{gpu_metropolis()}} samples it exactly and independently, with no warmup,
no proposal and no rejection; the effective sample size equals the number
of draws. The marginal likelihood is also available in closed form and is
returned by the fit as \code{log_marginal}.
}
\details{
The prior must be proper (\code{B0} positive definite, \code{c0 > 0}, \code{d0 > 0}) for
the marginal likelihood to exist; the sampling itself only needs the
posterior precision \verb{X'X + B0} to be positive definite. The defaults are
weakly informative and proper.
}
\examples{
m <- gpum_lm(mpg ~ wt, data = mtcars)
fit <- gpu_metropolis(m, n_iter = 4000, n_chains = 4, seed = 1)
fit$log_marginal

}
\seealso{
\code{\link[=gpu_metropolis]{gpu_metropolis()}}, \code{\link[=gpum_model]{gpum_model()}}
}
