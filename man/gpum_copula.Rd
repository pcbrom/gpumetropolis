% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula.R
\name{gpum_copula}
\alias{gpum_copula}
\title{Fit a bivariate copula to two-column data}
\usage{
gpum_copula(
  data,
  family = "auto",
  criterion = c("waic", "loo"),
  method = "mala",
  warmup = "auto",
  n_iter = 8000L,
  n_chains = 4L,
  seed = 1L,
  ...
)
}
\arguments{
\item{data}{A two-column data frame or matrix; the columns are the two
variables whose dependence is modelled.}

\item{family}{One of \code{"gumbel"}, \code{"clayton"}, \code{"frank"}, \code{"gaussian"}, or
\code{"auto"} to fit all four and select by predictive comparison. Default
\code{"auto"}.}

\item{criterion}{Selection criterion for \code{family = "auto"}: \code{"waic"}
(default) or \code{"loo"}. Lower expected log predictive density loss wins.}

\item{method}{Sampler passed to \code{\link[=gpu_metropolis]{gpu_metropolis()}}. Default \code{"mala"}: the
copula log-density is smooth and one-dimensional in the raw parameter,
and the Frank family in particular has a flat far tail in which a
random walk can strand a chain, which the gradient path avoids.}

\item{warmup}{Warmup passed to \code{\link[=gpu_metropolis]{gpu_metropolis()}}. Default \code{"auto"}.}

\item{n_iter, n_chains, seed, ...}{Passed to \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}
}
\value{
An object of class \code{gpum_copula}: a list with the selected
\code{family}, the \code{gpum_fit}, the posterior summaries of the dependence
parameter and of Kendall's tau and the tail-dependence coefficients,
the pseudo-observations, and (for \code{"auto"}) the \code{comparison} table.
}
\description{
Infers the dependence structure of two variables by fitting a bivariate
copula to their pseudo-observations (average ranks rescaled to the open
unit square), so the fit is invariant to the marginal distributions and
isolates the dependence. The copula parameter is sampled with
\code{\link[=gpu_metropolis]{gpu_metropolis()}}; the copula log-density is a normalised density on the
unit square, so WAIC and PSIS-LOO compare families directly.
}
\details{
With \code{family = "auto"} every candidate family is fit and the workflow
returns the one preferred by predictive comparison together with the full
ranking, since the choice of family is a hypothesis about tail behaviour.
Gumbel carries upper-tail dependence, Clayton lower-tail, Frank symmetric
and tail-independent, Gaussian elliptical and tail-independent; a
selected Gumbel is itself the statement that co-movement concentrates in
the upper tail.

The three Archimedean families (Gumbel, Clayton, Frank) are parametrised
for positive association in this release; the Gaussian family carries the
sign of the dependence through its correlation. For data with negative
rank correlation, the Gaussian family applies directly and the others do
not.
}
\examples{
set.seed(1)
u <- runif(300)
v <- pnorm(0.8 * qnorm(u) + 0.6 * rnorm(300))
fit <- gpum_copula(data.frame(x = qexp(u), y = qgamma(v, 2)),
                   family = "auto", n_iter = 4000, n_chains = 4)
fit

}
\seealso{
\code{\link[=gpu_metropolis]{gpu_metropolis()}}, \code{\link[=kendall_tau]{kendall_tau()}}, \code{\link[=tail_dependence]{tail_dependence()}}
}
