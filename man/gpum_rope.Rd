% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{gpum_rope}
\alias{gpum_rope}
\title{ROPE and HDI decision on one parameter}
\usage{
gpum_rope(fit, parameter, rope, null = 0, ci = 0.95)
}
\arguments{
\item{fit}{A \code{gpum_fit} from \code{\link[=gpu_metropolis]{gpu_metropolis()}}.}

\item{parameter}{Name of the parameter.}

\item{rope}{Either the half-width of a symmetric ROPE around \code{null}, or a
length-two vector giving the ROPE interval directly.}

\item{null}{Centre of a symmetric ROPE. Ignored when \code{rope} is an interval.
Default 0.}

\item{ci}{Mass of the highest density interval. Default 0.95.}
}
\value{
An object of class \code{gpum_rope} with the \code{hdi}, the \code{rope} interval,
the share \code{pct_in_rope} of the posterior inside it, and a \code{decision}.
}
\description{
Applies the region-of-practical-equivalence rule (Kruschke 2018): the
parameter is practically equivalent to the null when its highest density
interval lies inside the ROPE, practically different when the interval lies
entirely outside it, and undecided otherwise. The decision is from the
draws and needs no marginal likelihood.
}
\seealso{
\code{\link[=hdi]{hdi()}}, \code{\link[=gpum_hypothesis]{gpum_hypothesis()}}
}
