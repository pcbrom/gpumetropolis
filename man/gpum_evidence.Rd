% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{gpum_evidence}
\alias{gpum_evidence}
\title{Log marginal likelihood by thermodynamic integration}
\usage{
gpum_evidence(
  model,
  data,
  n_rungs = 24L,
  power = 5,
  n_iter = 4000L,
  n_chains = 8L,
  warmup = NULL,
  method = "de",
  seed = 1L,
  backend = "cpu"
)
}
\arguments{
\item{model}{A \code{gpum_model} with a proper \code{prior}.}

\item{data}{The data for the model.}

\item{n_rungs}{Number of intervals in the \code{beta} grid. Default 24.}

\item{power}{Exponent of the power-law grid \code{beta = (k / n_rungs)^power}.
Default 5, the value that controls the discretisation bias of trapezoidal
thermodynamic integration.}

\item{n_iter, n_chains, warmup, method, seed, backend}{Passed to
\code{\link[=gpu_metropolis]{gpu_metropolis()}} for the per-rung sampling. Defaults are 4000 iterations,
8 chains, \code{method = "de"}.}
}
\value{
An object of class \code{gpum_evidence} with \code{log_evidence}, the \code{betas}
grid and the \code{expected_loglik} per rung.
}
\description{
Estimates the log marginal likelihood (the model evidence) by
thermodynamic integration along a path of power posteriors
\verb{p_beta(theta) proportional to p(y | theta)^beta p(theta)} from the prior
(\code{beta = 0}) to the posterior (\code{beta = 1}). The log evidence is the integral
over \code{beta} of the expected log-likelihood under \code{p_beta} (Gelman and Meng
1998; Friel and Pettitt 2008), evaluated on a power-law grid that is dense
near the prior, where the integrand is most curved. Each rung is sampled by
the package's own sampler, so no kernel change is needed.
}
\details{
The evidence requires a proper prior: the marginal likelihood is an integral
against the prior, undefined for a flat or absent prior. The function stops
when the model has no prior term. The estimate is sensitive to the prior, a
property of the marginal likelihood itself, not of the sampler.
}
\seealso{
\code{\link[=gpum_bayes_factor]{gpum_bayes_factor()}}, \code{\link[=gpum_waic]{gpum_waic()}}
}
